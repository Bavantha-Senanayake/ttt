// Optional: Component-specific styles if needed for more complex styling
// This file can be used for StyleSheet styles that are more complex than Tailwind classes

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  messageText: {
    marginTop: 12,
    fontSize: 16,
    color: '#6B7280',
  },
});

export default styles;